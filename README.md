![alt text](https://github.com/DamirovichRK/infotecs-test/blob/master/Peperain.png "Logo Title Text 1")

# HTTP Benchmark Tool


Утилита для тестирования доступности веб-серверов с поддержкой последовательных и параллельных запросов.



## Особенности


- 📊 Статистика по каждому хосту (успешные/неудачные запросы, время ответа)

- ⚡ Поддержка параллельных запросов для ускорения тестирования

- 🔒 Встроенная валидация и проверка безопасности входных данных

- 📁 Поддержка чтения адресов из файла и сохранения результатов

- 🛠️ Гибкая настройка параметров тестирования


## Требования

- Python 3.6+

- requests>=2.28.0



```http
  pip install requests
```


## Установка

    1. Клонируйте репозиторий:



```bash
    git clone <repository-url>
    cd <repository-directory>
```

    2. Убедитесь, что установлены зависимости:

```bash
    pip install requests
```

    


## Использование
1. Тестирование одного хоста:

```bash
peperain.py -H https://example.com -C 5
```

2. Тестирование нескольких хостов:

```bash
python peperain.py -H https://ya.ru,https://google.com -C 3
```

3. Чтение адресов из файла:

```bash
python peperain.py -F urls.txt -C 5
```

4. Сохранение результатов в файл:

```bash
python peperain.py -H https://example.com -C 10 --output results.txt
```
## Параллельные запросы
### Использование параллельного режима:

```bash
python peperain.py -H https://site1.com,https://site2.com -C 5 --concurrent
```
### Настройка количества потоков:

```bash
python peperain.py -F urls.txt -C 3 --concurrent --workers 10
```

### Все параметры
-H, --hosts        Адреса для тестирования через запятую

-C, --count        Количество запросов на каждый хост (по умолчанию: 1)

-F, --file         Файл со списком адресов (каждый с новой строки)

-O, --output       Файл для сохранения результатов

--concurrent       Использовать параллельные запросы

--workers          Количество потоков для параллельных запросов (по умолчанию: 5)

--timeout          Таймаут запроса в секундах (по умолчанию: 10.0)

## Примеры файлов
### Файл со списком URL (urls.txt):
text

https://example.com

https://google.com

https://github.com

## Особенности реализации
### Безопасность
Проверка на инъекции и опасные символы

Валидация URL формата

Защита от SSRF (локальные адреса, метаданные облаков)

Проверка файловых путей

### Производительность
Поддержка до 100 параллельных потоков

Оптимизированное использование памяти

Таймауты для предотвращения зависаний

### Надежность
Обработка всех типов ошибок

Корректное завершение при прерывании

Логирование ошибок в консоль

### Ограничения
Максимальное количество хостов: 100

Максимальное количество запросов на хост: 10,000

Максимальное количество потоков: 100

Поддерживаются только HTTPS URL

Максимальный размер входного файла: 10 МБ

## Структура проекта
InputValidator - класс для валидации входных данных

HTTPRequest - класс для выполнения HTTP-запросов

Основной скрипт peperain.py - точка входа

## Тестирование
Для тестирования можно использовать различные комбинации параметров:


### Быстрый тест
python peperain.py -H https://httpbin.org/status/200 -C 3

### Тест с ошибками
python peperain.py -H https://httpbin.org/status/404,https://httpbin.org/status/500 -C 2

### Параллельный тест
python peperain.py -H https://httpbin.org/delay/1,https://httpbin.org/delay/2 -C 3 --concurrent

## Примечания
Для работы с большим количеством хостов рекомендуется использовать параллельный режим

При тестировании внешних сервисов соблюдайте их ограничения по частоте запросов

Результаты сохраняются в текстовом формате для удобства обработки

